{
  "routes": [
    { "src": "/dashboard", "dest": "/dashboard" },
    { "src": "/staking", "dest": "/staking" },
    { "src": "/referral", "dest": "/referral" },
    { "src": "/(.*)", "dest": "/" }
  ],
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ]
}